import { Box } from "@mantine/core";
import { Table } from "./components/table";

// export const meta: Route.MetaFunction = () => [{ title: "YMMA Schedule" }];

export default function YmmaSchedule() {
  return (
    <Box>
      <Table />
    </Box>
  );
}

import { Box } from "@mantine/core";
import { Table } from "./components/table";

// export const meta: Route.MetaFunction = () => [{ title: "WH Receipt Line" }];

export default function WHReceiptLine() {
  return (
    <Box>
      <Table />
    </Box>
  );
}
